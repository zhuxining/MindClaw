# MindClaw 技术架构设计 — SubAgent 与扩展性

> 完整架构文档索引见 [README.md](./README.md) | Agent 核心见 [05-agent.md](./05-agent.md)

## 架构设计原则

Agent 扩展机制遵循**分层解耦、按需发现、类型安全**原则：

- **Tool**：原子能力单元，统一抽象（本地函数、MCP、Skill 注入）
- **Skill**：能力组合包，可同时提供 Tool + ContextSource + Hook
- **MCP**：外部工具协议，Agent 作为 Client 集成
- **Hook**：事件拦截点，支持三态决策（Continue/Apply/Block）
- **SubAgent**：多 Agent 编排，支持后台异步 + 同步委托双模式

---

## 6.11 MCP 集成 — 外部工具协议

MindClaw 作为 MCP Client，连接外部 MCP Server 获取工具能力。与业界最佳实践对齐，**每个 MCP tool 独立注册到 ToolRegistry**，而非封装为单一 `mcp_client` 工具。

### MCP 架构

```
McpServerManager
    ├── connects to MCP servers (stdio / HTTP)
    ├── discovers available tools via tools/list
    └── registers McpTool instances to ToolRegistry

ToolRegistry
    ├── resident: filesystem, shell, operations
    └── discoverable: mcp__{server}__{tool} (按需发现)
```

### MCP Server Manager

```rust
// src-tauri/src/tools/mcp.rs

/// MCP 传输层配置
pub enum McpTransport {
    Stdio { command: String, args: Vec<String> },
    Http { url: String },
}

/// MCP Server 配置
pub struct McpServerConfig {
    pub name: String,
    pub transport: McpTransport,
    pub auto_register: McpAutoRegister,
}

/// MCP 工具自动注册策略
pub enum McpAutoRegister {
    None,               // 全部按需发现
    All,                // 全部注册为常驻
    Only(Vec<String>),  // 仅白名单工具注册为常驻
}

/// MCP Server 管理器
pub struct McpServerManager {
    servers: HashMap<String, McpServerConfig>,
    clients: HashMap<String, Arc<McpClient>>,
}

impl McpServerManager {
    /// 连接所有配置的服务器并发现工具
    pub async fn discover_and_register(&self, registry: &mut ToolRegistry) -> Result<(), AppError>;
}
```

### McpTool — 独立 Tool 实现

```rust
/// 从 MCP Server 动态发现的工具
pub struct McpTool {
    server_name: String,
    tool_name: String,
    description: String,
    schema: Value,
    client: Arc<McpClient>,
}

#[async_trait]
impl Tool for McpTool {
    fn name(&self) -> &str {
        // 命名空间格式: mcp__{server}__{tool}
        &self.full_name  // e.g., "mcp__filesystem__read_file"
    }

    fn description(&self) -> &str { &self.description }
    fn input_schema(&self) -> Value { self.schema.clone() }

    async fn execute(&self, input: ToolInput) -> AppResult<ToolOutput> {
        // 通过 MCP 协议调用远端工具
        self.client.call_tool(&self.tool_name, input.parameters).await
    }
}
```

### MCP 工具发现

MCP tools 默认**不常驻上下文**，通过 `operations` 元工具按需发现：

```json
// operations list 返回
{
  "namespaces": {
    "mcp": [
      { "name": "mcp__filesystem__read_file", "description": "..." },
      { "name": "mcp__filesystem__write_file", "description": "..." }
    ],
    "services": [...],
    "agents": [...]
  }
}
```

用户可通过配置将常用 MCP tools 提升为常驻：

```toml
# settings.toml
[mcp.servers.filesystem]
transport = { type = "stdio", command = "npx", args = ["-y", "@modelcontextprotocol/server-filesystem", "/vault"] }
auto_register = ["read_file", "list_directory"]  # 常驻工具白名单
```

约定：

- `auto_register = "none"`：全部走按需发现
- `auto_register = "all"`：该 server 的全部 tools 常驻注册
- `auto_register = ["read_file", "list_directory"]`：仅白名单常驻，其余仍通过 `operations` 发现

---

## 6.12 SubAgent — 多 Agent 编排

SubAgent 支持**两种执行模式**：

1. **Background** — 后台异步任务，RunCompleted 后派发，不阻塞对话
2. **Delegate** — 同步委托执行（Agent-as-Tool），LLM 调用等待结果返回

```
AgentLoop (主对话)
    │
    ├── 对话响应 → 立即返回给用户
    │
    ├── 派发 Background SubAgent（不阻塞）
    │   ├── KnowledgeDistill:  从对话中提炼知识笔记
    │   ├── SessionSummarize:  会话摘要生成
    │   └── DailySummary:      当日回顾生成
    │
    └── Delegate SubAgent（同步调用，Agent-as-Tool）
        ├── Explore:   只读探索子代理（Haiku 模型）
        ├── Plan:      规划子代理（继承主模型）
        └── Execute:   执行子代理（带权限隔离）
```

为满足最小权限原则与可运维性，SubAgent 不仅区分模式，还必须声明**执行契约**：能力范围、时间/成本预算、取消策略、文件系统与网络边界。

### SubAgentTask 与执行器

```rust
// src-tauri/src/agent/sub_agent.rs

/// 子代理执行模式
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SubAgentMode {
    Background,  // 后台异步
    Delegate,    // 同步委托 (Agent-as-Tool)
}

/// 文件系统访问范围
pub enum FsScope {
    ReadOnly(Vec<PathBuf>),
    ReadWrite(Vec<PathBuf>),
    None,
}

/// 网络访问策略
pub enum NetworkPolicy {
    Deny,
    Allowlist(Vec<String>),  // host allowlist
}

/// 提权/审批策略
pub enum ApprovalPolicy {
    Inherit,
    Never,
    ExplicitPerAction,
}

/// 子代理能力画像
pub struct CapabilityProfile {
    pub allowed_tools: Vec<String>,
    pub fs_scope: FsScope,
    pub network: NetworkPolicy,
    pub approval_policy: ApprovalPolicy,
    pub max_tokens: Option<u32>,
    pub max_tool_calls: u32,
    pub timeout_ms: u64,
    pub max_retries: u8,
}

/// 子代理任务
pub struct SubAgentTask {
    pub id: String,
    pub task_type: String,
    pub session_id: String,
    pub parameters: serde_json::Value,
    pub priority: i32,
    pub mode: SubAgentMode,
    pub preferred_model: Option<String>,  // e.g., "claude-3-5-haiku"
    pub capabilities: CapabilityProfile,
    pub cancellation_token: CancellationToken,
    pub dedupe_key: Option<String>,
}

/// 子代理执行结果
pub struct SubAgentResult {
    pub success: bool,
    pub output: serde_json::Value,  // Delegate 模式返回给 LLM
    pub summary: String,            // Background 模式日志摘要
    pub logs: Vec<String>,
    pub artifacts: Vec<Artifact>,   // 生成的产物（知识草稿、摘要等）
}

pub struct Artifact {
    pub kind: String,    // "knowledge_draft" | "summary" | "observation"
    pub content: String,
    pub metadata: Value,
}

/// 后台任务状态
pub enum SubAgentJobState {
    Queued,
    Running,
    Succeeded,
    Failed,
    Retrying,
    Cancelled,
}

/// 持久化后台任务
pub struct SubAgentJob {
    pub task: SubAgentTask,
    pub state: SubAgentJobState,
    pub attempts: u8,
    pub last_error: Option<String>,
    pub next_run_at: Option<DateTime<Utc>>,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}

/// 子代理执行器 trait
#[async_trait]
pub trait SubAgentExecutor: Send + Sync {
    fn task_type(&self) -> &str;

    /// 支持的执行模式（默认仅 Background）
    fn supported_modes(&self) -> Vec<SubAgentMode> {
        vec![SubAgentMode::Background]
    }

    async fn execute(&self, task: &SubAgentTask) -> Result<SubAgentResult, AppError>;
}
```

### 后台任务队列与恢复

Background SubAgent 不是内存级 fire-and-forget，而是**持久化任务队列**：

- `dispatch_background()` 只负责入队，成功条件是任务已写入 durable store
- Worker 从队列拉取 `Queued/Retrying` 任务执行，进程重启后可恢复
- 失败任务按指数退避重试；达到上限后转 `Failed`
- 支持 `dedupe_key` 去重，避免同一会话重复生成摘要/知识笔记
- 支持取消、超时和并发配额，避免后台任务挤占前台交互资源

### SubAgentRegistry

```rust
pub struct SubAgentRegistry {
    executors: HashMap<String, Box<dyn SubAgentExecutor>>,
    job_store: Arc<dyn SubAgentJobStore>,
    background_queue: Arc<dyn SubAgentQueue>,
}

impl SubAgentRegistry {
    /// 注册执行器
    pub fn register(&mut self, executor: Box<dyn SubAgentExecutor>);

    /// 后台派发（durable enqueue）
    pub async fn dispatch_background(&self, task: SubAgentTask) -> Result<JobId, AppError>;

    /// 同步委托执行（等待结果返回）
    pub async fn dispatch_delegate(&self, task: SubAgentTask) -> Result<SubAgentResult, AppError>;
}
```

`dispatch_delegate()` 执行前必须校验：

- 调用的 executor 是否支持 `Delegate`
- `capabilities.allowed_tools` 是否满足最小权限
- `fs_scope` / `network` / `approval_policy` 是否与当前 channel 和运行模式兼容
- `timeout_ms` / `max_tokens` / `max_tool_calls` 是否在系统上限内

### Agent-as-Tool：DelegateTool

将 SubAgent 包装为 Tool，供 LLM 同步调用：

```rust
// src-tauri/src/tools/delegate.rs

pub struct DelegateTool {
    executor_type: String,
    description: String,
    input_schema: Value,
    registry: Arc<SubAgentRegistry>,
}

#[async_trait]
impl Tool for DelegateTool {
    fn name(&self) -> &str {
        // 命名空间: agent__{type}
        &self.name  // e.g., "agent__explore"
    }

    async fn execute(&self, input: ToolInput) -> AppResult<ToolOutput> {
        let task = SubAgentTask {
            id: Uuid::new_v4().to_string(),
            session_id: input.session_id.clone(),
            task_type: self.executor_type.clone(),
            mode: SubAgentMode::Delegate,
            parameters: input.parameters,
            capabilities: CapabilityProfile::delegate_default(&self.executor_type),
            cancellation_token: CancellationToken::new(),
            dedupe_key: None,
            preferred_model: None,
            priority: 0,
        };

        let result = self.registry.dispatch_delegate(task).await?;
        Ok(ToolOutput::ok(result.output))
    }
}
```

Delegate tools 默认**可发现**（非常驻），通过 `operations list` 在 `agents` 命名空间暴露。

---

## 6.13 Agent Hooks — 事件钩子系统

Hooks 在 Agent 执行关键节点提供拦截能力，支持**继续 / 显式变换 / 阻断**。为保证可组合性，修改必须通过结构化 patch 表达，而不是依赖 `&mut Context` 的隐式副作用。

### Hook 生命周期事件

```rust
// src-tauri/src/agent/hooks.rs

pub enum HookPoint {
    SessionStart,   // 会话创建时
    PreMessage,     // 处理消息前（可修改/阻止）
    PostMessage,    // 处理消息后（副作用触发）
    PreToolUse,     // 工具调用前（可验证/修改参数）
    PostToolUse,    // 工具调用后（可审计/修改输出）
    RunCompleted,   // Run 成功完成
    RunFailed,      // Run 失败
    SessionEnd,     // 会话结束时
}
```

### Hook 修改模型

```rust
pub enum HookPatch {
    ReplaceMessage { content: String },
    ReplaceToolInput { input: Value },
    ReplaceToolOutput { output: ToolOutput },
    AddMetadata { key: String, value: String },
}
```

### Hook 决策类型

```rust
pub enum HookDecision {
    /// 继续正常执行
    Continue,
    /// 应用 patch 后继续；多个 hook 按 priority 顺序依次合并
    Apply(Vec<HookPatch>),
    /// 阻止执行并给出理由
    Block(String),
}
```

### Hook Trait

```rust
pub enum HookPayload {
    Message { content: String },
    ToolUse { tool_name: String, input: Value },
    ToolResult { tool_name: String, output: ToolOutput },
    Lifecycle,
}

pub struct HookContext {
    pub session_id: String,
    pub hook_point: HookPoint,
    pub payload: HookPayload,
    pub metadata: HashMap<String, String>,
}

#[async_trait]
pub trait Hook: Send + Sync {
    fn name(&self) -> &str;
    fn hook_point(&self) -> HookPoint;
    fn priority(&self) -> i32 { 0 }  // 数值越小越先执行

    async fn execute(&self, ctx: &HookContext) -> Result<HookDecision, AppError>;
}
```

### HookRegistry

```rust
pub struct HookRegistry {
    hooks: Vec<Box<dyn Hook>>,
}

impl HookRegistry {
    pub fn register(&mut self, hook: Box<dyn Hook>);

    /// 执行指定事件点的所有 hooks
    /// 遇到 Block 立即返回；Apply 按优先级顺序依次合并
    pub async fn run_hooks(
        &self,
        point: HookPoint,
        ctx: HookContext,
    ) -> Result<HookDecision, AppError>;
}
```

合并规则：

- 多个 `Apply` 顺序执行，后一个 patch 基于前一个 patch 的结果
- 同一字段发生冲突时，后执行的高优先级 hook 覆盖前者
- 每次 patch 应用后记录审计日志，支持回放与故障排查

### Hook 触发点在 AgentLoop 中的位置

```
run_once():
  1. Session get_or_create ──► run_hooks(SessionStart)
  2. Agent Command interception
  3. ► run_hooks(PreMessage) ◄
     └── 可 Block（阻止处理）或 Apply（修改消息内容）
  4. ContextPipeline.build()
  5. Provider chat_stream()
  6. 工具循环内：
     ├── ► run_hooks(PreToolUse) ◄
     │   └── 可 Block（阻止工具执行）或 Apply（修改参数）
     ├── tools.execute_calls()
     └── ► run_hooks(PostToolUse) ◄
         └── 可 audit / modify output
  7. Session append
  8. ► run_hooks(PostMessage) ◄
  9. RunCompleted / RunFailed ──► run_hooks(RunCompleted/RunFailed)
  10. Session close ──► run_hooks(SessionEnd)
```

### Shell 命令钩子（受控执行）

无需编译的轻量级 Hook，通过配置启用；但必须使用**结构化命令定义**，禁止直接执行模板化 shell 字符串。

```rust
pub enum HookFailurePolicy {
    FailOpen,    // hook 执行异常时仅记录日志
    FailClosed,  // hook 执行异常时阻断主流程
}

pub struct CommandHook {
    pub name: String,
    pub hook_point: HookPoint,
    pub matcher: Option<String>,  // 工具名正则匹配（仅 PreToolUse/PostToolUse）
    pub command: String,          // 可执行文件绝对路径
    pub args: Vec<String>,        // 结构化参数，支持 ${session_id} 等变量插值
    pub env: HashMap<String, String>,
    pub cwd: Option<PathBuf>,
    pub timeout_ms: u64,
    pub priority: i32,
    pub failure_policy: HookFailurePolicy,
}
```

```toml
# settings.toml
[[hooks]]
name = "audit-shell"
hook_point = "PreToolUse"
matcher = "shell"
command = "/usr/bin/logger"
args = ["mindclaw-hook", "tool=${tool_name}", "input=${tool_input_json}"]
timeout_ms = 1000
priority = 100
failure_policy = "fail_open"

[[hooks]]
name = "policy-block-network-write"
hook_point = "PreToolUse"
matcher = "shell|mcp__.*__write.*"
command = "/Users/me/.mindclaw/hooks/check-policy"
args = ["--tool", "${tool_name}", "--input", "${tool_input_json}"]
timeout_ms = 2000
priority = 10
failure_policy = "fail_closed"
```

安全要求：

- 不经 shell 解析，不支持 `$(...)`、管道、重定向、通配符展开
- 变量插值仅发生在 `args/env` 值内，且按参数边界传递
- 可配置允许的可执行文件目录，默认仅允许运行受信任路径
- `fail_closed` 仅用于策略/审批类 hook；审计类 hook 默认 `fail_open`

Command hook 的退出码映射到 `HookDecision`：

- `0` → Continue
- `2` → Block（stderr/stdout 简述作为理由）
- 其他 → 按 `failure_policy` 处理：`FailOpen` 记录错误后继续，`FailClosed` 阻断主流程

---

## 6.14 Agent Skills — 技能系统

Skill 是 Agent 能力的组合包，遵循 [Agent Skills 标准](https://agentskills.io) 的三级渐进式披露设计：Catalog → Instructions → Resources。

### 核心原则：渐进式披露

```
Tier 1: Catalog (Session Start)
├── name + description 注入 system prompt
└── ~50-100 tokens per skill

Tier 2: Instructions (On Activation)
├── 完整 SKILL.md body
├── 由 ContextSource 在预算允许时直接注入
└── <5000 tokens (recommended)

Tier 3: Resources (On Demand)
├── scripts, references, assets
├── 模型通过 file-read 按需加载
└── Varies by usage
```

### Skill Discovery — 运行时扫描

MindClaw 支持**双模式**：内置 skills（编译期嵌入）+ 运行时扫描（文件系统）。

**扫描路径**（按优先级排序）：

| 优先级 | 来源 | 路径 | 说明 |
|--------|------|------|------|
| 1 | Builtin | `src-tauri/skills/*.md` | 编译期嵌入，开箱即用 |
| 2 | Project | `<project>/.agents/skills/` | 项目级自定义 |
| 3 | User | `~/.agents/skills/` | 用户级共享 |
| 4 | Compatibility | `~/.claude/skills/` | 兼容现有生态 |

**冲突处理**：Project > User > Builtin。同名 skill 高优先级覆盖低优先级，记录 warning 日志。

**信任检查**：Project-level skills 来自仓库，首次扫描时提示用户确认信任，防止恶意代码注入。

```rust
// src-tauri/src/agent/skill_discovery.rs

pub struct SkillDiscovery {
    builtin: HashMap<String, SkillDoc>,
    project: HashMap<String, SkillDoc>,
    user: HashMap<String, SkillDoc>,
}

impl SkillDiscovery {
    /// 扫描所有来源
    pub async fn scan_all(&mut self, project_root: &Path) -> Result<(), AppError> {
        // 1. 内置 skills（编译期）
        self.load_builtin();

        // 2. User-level: ~/.agents/skills/
        if let Ok(dir) = home_dir().join(".agents/skills").canonicalize() {
            self.scan_directory(&dir, SkillSource::User).await?;
        }

        // 3. Project-level: <project>/.agents/skills/（需信任确认）
        let project_skills = project_root.join(".agents/skills");
        if project_skills.exists() && self.is_project_trusted(project_root) {
            self.scan_directory(&project_skills, SkillSource::Project).await?;
        }

        // 4. 兼容: ~/.claude/skills/
        if let Ok(dir) = home_dir().join(".claude/skills").canonicalize() {
            self.scan_directory(&dir, SkillSource::Compatibility).await?;
        }

        Ok(())
    }

    /// 按优先级解析 skill
    pub fn resolve(&self, name: &str) -> Option<&SkillDoc> {
        self.project.get(name)
            .or_else(|| self.user.get(name))
            .or_else(|| self.builtin.get(name))
    }

    /// 收集所有技能用于 Catalog（Tier 1）
    pub fn collect_catalog(&self) -> Vec<SkillCatalogItem> {
        // 按优先级合并，去重
    }
}

#[derive(Debug, Clone, Copy)]
pub enum SkillSource {
    Builtin,
    Project,
    User,
    Compatibility,  // .claude/skills/
}
```

### Skill 文件格式

标准 `SKILL.md` 格式（YAML frontmatter + Markdown body）：

```markdown
---
name: pdf-processing
description: "Extract PDF text, fill forms, merge files. Use when handling PDFs."
version: "1.0.0"
compatibility: Python 3.9+, PyPDF2
---

# PDF Processing

## When to use this skill
Use this skill when the user needs to work with PDF files...

## Available operations
...

## Bundled resources
- `scripts/extract.py`: Text extraction script
- `references/pdf-spec-summary.md`: PDF format reference
```

**解析容错**：

- YAML 解析失败 → 跳过 skill，记录 error
- `name` 不匹配目录名 → warning，继续加载
- `description` 缺失 → 跳过 skill（无法披露）
- 未引号的冒号值（如 `description: Use when: the user...`）→ 自动添加引号重试

### 三级披露实现

#### Tier 1: Catalog

Session start 时注入 system prompt：

```rust
// SkillsOverviewSource (ContextSource)
pub struct SkillsOverviewSource {
    catalog: Vec<SkillCatalogItem>,
}

#[async_trait]
impl ContextSource for SkillsOverviewSource {
    async fn inject(&self, _ctx: &ContextBuildContext, _budget: usize)
        -> Result<Vec<ContextFragment>, AppError> {

        let content = format!(
            "Available skills:\n\n{}",
            self.catalog.iter()
                .map(|s| format!("- {}: {}", s.name, s.description))
                .collect::<Vec<_>>()
                .join("\n")
        );

        Ok(vec![ContextFragment::system(content, "skills_catalog")])
    }
}
```

#### Tier 2: Instructions

Skill instructions 不作为 Tool 暴露，而是作为**上下文能力**注入：

```rust
pub struct SkillsOverviewSource {
    catalog: Vec<SkillCatalogItem>,
    builtin_docs: HashMap<String, SkillDoc>,
}

#[async_trait]
impl ContextSource for SkillsOverviewSource {
    async fn inject(&self, _ctx: &ContextBuildContext, budget: usize)
        -> Result<Vec<ContextFragment>, AppError> {

        let content = render_skills_context(&self.catalog, &self.builtin_docs, budget);
        Ok(vec![ContextFragment::system(content, "skills_catalog")])
    }
}
```

设计原则：

- Skill 是 prompt-side guidance，不是 callable tool
- 小型内置技能可在预算允许时直接注入全文
- 大型外部技能仅注入 Catalog，具体资源仍通过文件系统按需读取

#### Tier 3: Resources

Skill instructions 中的相对路径解析为绝对路径，模型通过 `filesystem` 工具按需读取：

```
skill 内容引用: "Use `scripts/extract.py` to process PDFs"
                    ↓
模型调用: filesystem read_file(path: "/home/user/.agents/skills/pdf-processing/scripts/extract.py")
                    ↓
返回脚本内容，执行后获得结果
```

### 激活方式

**Model-driven（主要）**：
模型看到 Catalog → 在预算允许时直接获得内置 skill instructions → 按 skill 指南调用 `operations` 或读取资源

**User-explicit（辅助）**：
用户输入 `/skill pdf-processing` → Agent Command 拦截 → 直接注入 skill content

### Skill 上下文保护

Skill instructions（Tier 2）一旦加载，应**免于 context compaction**：

- Skill content 标记为 protected，pruning 算法跳过
- 或通过结构化标签 `<skill_content>` 识别并保留
- 防止 mid-conversation 丢失 skill instructions 导致行为退化

### 双模式技能架构

```
SkillRegistry
├── Builtin Skills (StandardSkillsBundle)
│   ├── Compile-time embedded via include_str!()
│   ├── Always available (even in sandboxed env)
│   └── Tier 1/2/3 supported
│
└── Runtime Skills (SkillDiscovery)
    ├── Project-level: .agents/skills/
    ├── User-level: ~/.agents/skills/
    ├── Compatibility: ~/.claude/skills/
    └── Trust check for project-level
```

**Code-based Skills**（Rust trait）：

- 通过 `Skill` trait 实现
- 编译期类型安全
- 适合核心能力（如内置 ContextSource / Hook / Operation）

**Prompt-based Skills**（Markdown）：

- 运行时扫描加载
- 零编译、即时修改
- 适合用户自定义、生态共享

### 与 SubAgent 的集成

Skill 可以在独立 SubAgent 中运行（标准中的 delegation 模式）：

```yaml
---
name: complex-analysis
description: "Multi-step data analysis"
context: fork  # 在独立 subagent 中运行
model: claude-3-5-haiku  # 使用轻量模型
---
```

激活时：

1. 创建独立 SubAgent session
2. 注入 skill instructions
3. 执行完成后返回 summary 给主会话

避免污染主对话上下文，适合复杂多步骤 workflow。

---

## 6.15 ToolRegistry — 工具注册与 Schema 管理

ToolRegistry 管理所有可用工具，支持**常驻**与**可发现**两种注册模式。

### 常驻 vs 可发现

```rust
pub struct ToolRegistry {
    tools: HashMap<String, Arc<dyn Tool + Send + Sync>>,
    resident_tools: HashSet<String>,  // 常驻工具名集合
    concurrency: Arc<Semaphore>,
}

impl ToolRegistry {
    /// 注册常驻工具（Schema 始终发送给 LLM）
    pub fn register_resident(&mut self, tool: Box<dyn Tool + Send + Sync>);

    /// 注册可发现工具（通过 operations list 暴露）
    pub fn register_discoverable(&mut self, tool: Box<dyn Tool + Send + Sync>);

    /// 获取常驻工具的 Schema（发送给 LLM）
    pub fn resident_schemas(&self) -> Vec<ToolSchema>;

    /// 获取所有工具的 Schema（用于 operations list）
    pub fn all_schemas(&self) -> Vec<ToolSchema>;
}
```

### 默认常驻 Schema（3 个）

| 工具 | 用途 |
|------|------|
| `filesystem` | 文件系统操作 |
| `shell` | 受限命令执行 |
| `operations` | 元工具（发现 Services/MCP/Agents） |

### 可发现工具

- **MCP tools**：`mcp__{server}__{tool}` 命名空间
- **Service operations**：`svc__{service}__{operation}` 命名空间
- **Delegate agents**：`agent__{type}` 命名空间

### Schema 层级控制

```
AgentLoop::run_once()
    │
    ├── resident_schemas() → 发送给 LLM（3 个）
    │
    └── operations list → 返回所有可发现工具
        │
        └── LLM 调用 operations call → 执行任意可发现工具
```

常驻 Schema 数量固定为 3，避免上下文膨胀；可发现工具数量无限制，按需使用。

---

## 6.16 Gateway Layer — HTTP/WebSocket 服务

Gateway 为移动端 PWA 提供静态文件和 API，为 Webhook 通道提供接入点。通过 Bus 解耦，不直接引用 Agent。

| 端点 | 方法 | 说明 | Phase |
|------|------|------|-------|
| `/api/chat` | POST | 发送消息，返回 Agent 响应 | Phase 1 后期 |
| `/api/daily/:date` | GET | 获取日记内容 | Phase 2 |
| `/api/knowledge` | GET | 知识库搜索 | Phase 2 |
| `/api/tasks` | GET | 任务列表 | Phase 2 |
| `/ws/chat` | WS | WebSocket 实时对话 | Phase 2 |
| `/webhook/telegram` | POST | Telegram Bot Webhook | Phase 1 后期 |
| `/webhook/feishu` | POST | 飞书 Bot Webhook | Phase 2 |
| `/` | GET | PWA 静态文件服务 | Phase 2 |

**认证**：

| 场景 | 认证方式 | 说明 |
|------|---------|------|
| 本地 WiFi（PWA /api/*） | Bearer Token | Token 存储在 OS Keychain |
| Tailscale 远程接入 | 双重保护 | Bearer Token + Tailscale 身份验证 |
| Webhook（Telegram/Feishu） | 平台签名验证 | 验证平台签名，**不需要** Bearer Token |
| WebSocket（/ws/chat） | Bearer Token | 连接时认证，之后保持会话 |

---

## 6.17 Cron — 定时任务调度

| 任务 | 默认频率 | 说明 | Phase |
|------|---------|------|-------|
| `daily_summary` | 每日 22:00 | 生成当日回顾，写入日记 | MVP |
| `resource_process` | 每 5 分钟 | 处理 pending 资源（解析 + 结晶） | MVP |
| `history_prune` | 每日 03:00 | 压缩旧对话历史，超 90 天转冷归档 | Phase 2 |
| `knowledge_review` | 每周日 10:00 | 回顾知识库，发现新关联 | Phase 2 |
| `index_rebuild` | 每日 04:00 | 增量重建 Markdown → SQLite 索引 | MVP |
| `memory_surface` | 每日 09:00 | 检查未浮出记忆的浮出时机 | Phase 2 |
| `heartbeat_check` | 每 30 秒 | 系统健康检测 | MVP |

基于 `tokio-cron-scheduler` 精确调度。

---

## 6.18 Heartbeat — 健康检测

```rust
pub struct SystemHealth {
    pub status: HealthStatus,          // healthy | degraded | down
    pub db_connected: bool,
    pub api_key_valid: bool,
    pub vault_accessible: bool,
    pub gateway_running: bool,
    pub channels: Vec<ChannelHealth>,
    pub last_check: DateTime<Utc>,
    pub uptime_seconds: u64,
}
```

通道断线时自动重连，指数退避（2s → 4s → 8s → ... → 60s）。

---

## 扩展性分阶段实现

| 组件 | MVP | Phase 1 | Phase 2 |
|------|-----|---------|---------|
| **MCP Client** | Y | Stdio/HTTP 传输 | Server 管理 UI |
| **SubAgent (Background)** | Y | 内置任务 | 自定义任务 |
| **SubAgent (Delegate)** | | Y | Agent-as-Tool 完整支持 |
| **Hooks (Rust)** | Y | 8 事件点 | Priority 精细控制 |
| **Hooks (Shell)** | | Y | settings.toml 配置 |
| **Skills (Built-in)** | Y | Markdown 技能 | 热加载 |
| **Skills (External)** | | | WASM 扩展 |
| **Gateway** | | Y | HTTP API |
| **WebSocket** | | | Y |

---

## 架构对比：MindClaw vs 业界

| 特性 | MindClaw | OpenAI Agents | Claude Code | LangGraph |
|------|----------|---------------|-------------|-----------|
| Tool 抽象 | `ToolRegistry` | `tools=[]` 列表 | 硬编码 + MCP | `BaseTool` |
| MCP 集成 | Client, 独立注册 | `MCPServerStdio` | `.mcp.json` | mcp-adapters |
| Skills | Rust trait + Markdown | 无 (namespace) | `SKILL.md` | 无 |
| Hooks | 8 事件 + 三态 | Guardrails | 8 事件 + 多 handler | Interrupts |
| SubAgent | 后台 + 委托 | Handoffs + Agent-as-Tool | Subagents | Checkpointing |
| 常驻 Schema | 4 个 + 可配置 | 全部 | 全部 | 全部 |
| Context 预算 | Token budget | 无 | 无 | 无 |
| 三级检索 | L0/L1/L2 | 无 | 无 | 无 |

**MindClaw 差异化**：Context Pipeline Token 预算、三级知识检索、事件驱动 MessageBus、严格的常驻 Schema 控制。
