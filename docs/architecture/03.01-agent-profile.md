> **Status**: `active`

# AgentProfile — 静态定义层

---

## § 职责定位

`AgentProfile` 负责定义某类 Agent 的身份、策略和硬边界，不负责任何运行时状态、上下文组装、模型调用或工具执行。

---

## § 核心原则

**Profile 不是 Instance**：Profile 描述“应该如何运行”，不是“正在运行什么”。

**策略与状态分离**：Session、消息历史、取消状态、流式输出句柄等均属于 Runtime，不属于 Profile。

---

## § 结构

```text
AgentProfile
├── PromptPolicy
├── ModelPolicy
├── ToolPolicy
├── ContextPolicy
├── ExecutionPolicy
├── InteractionPolicy   ← 新增：交互模式策略
├── SecurityPolicy
├── DelegationPolicy
└── InvocationPolicy
```

---

## § 核心字段

**基础身份**

- `agent_id`
- `kind`：`main` / `sub` / `background`
- `name`
- `description`

**PromptPolicy**

- `system_prompt_template`
- `prompt_version`
- `extra_instructions`

**ModelPolicy**

- `preferred_model`
- `fallback_models`
- `provider_allowlist`
- `required_capabilities`
- `temperature`
- `max_tokens`
- `reasoning_effort`

**ToolPolicy**

- `allowed_tools`
- `tool_choice`
- `parallel_tools`
- `max_tool_calls`
- `tool_timeout_ms`
- `fail_on_tool_error`

**ContextPolicy**

- `context_budget`
- `history_policy`
- `memory_policy`
- `compression_policy`
- `include_knowledge`
- `include_skills`

**ExecutionPolicy**

- `max_iterations`
- `streaming_policy`
- `output_format`
- `response_schema`
- `stop_sequences`

**InteractionPolicy** — 新增章节

定义 Agent 的交互模式及其策略参数。模式不是硬编码的，而是通过配置定义的行为策略组合。

```rust
struct InteractionPolicy {
    default_mode: String,           // 默认模式 ID
    mode_switching: ModeSwitching,  // manual | auto | disabled
    mode_definitions: Vec<ModeDefinition>,
}

enum ModeSwitching {
    Manual,     // 用户显式切换
    Auto,       // 基于触发条件自动建议
    Disabled,   // 固定使用 default_mode
}

struct ModeDefinition {
    mode_id: String,                // 唯一标识，如 "companion"
    name: String,                   // 显示名称
    description: String,            // 模式说明
    system_prompt_addon: String,    // 该模式附加到 system prompt 的内容
    allowed_tools: Vec<String>,     // 该模式下允许的工具白名单（空=继承全部）
    trigger_keywords: Vec<String>,  // 自动触发关键词（mode_switching=auto 时有效）
    visibility: ContentVisibility,  // 内容可见性边界
}

enum ContentVisibility {
    User,           // 内容对用户可见，可入知识库
    AgentPrivate,   // 仅 Agent 私有，不入知识库
}
```

**默认模式模板**（系统提供，可覆盖）：

| 模式 ID | 名称 | 策略特征 |
|---------|------|---------|
| `companion` | 陪伴模式 | 共情优先、`allowed_tools: []`、不主动调用工具 |
| `reflection` | 反思模式 | 提问引导、可调用知识检索工具 |
| `challenge` | 挑战模式 | 直接反馈、可调用分析工具 |
| `knowledge` | 知识模式 | 结构化输出、可写入知识库 |
| `vault` | 树洞模式 | 私密、`visibility: AgentPrivate` |

**使用方式**：

1. AgentLoop 在构建 `AgentRunSpec` 时读取当前 mode
2. 将 `system_prompt_addon` 追加到 base system prompt
3. ToolExecutor 根据 `allowed_tools` 过滤可用工具
4. 持久化层根据 `visibility` 决定是否写入知识库

**SecurityPolicy**

- `workspace_roots`
- `path_policy`
- `permission_profile`
- `network_policy`

**DelegationPolicy**

- `allow_subagents`
- `allow_background_agents`
- `allowed_child_agents`
- `max_child_depth`
- `max_parallel_children`
- `child_timeout_ms`

**InvocationPolicy**

- `callable_by`
- `inherits_workspace_scope`
- `inherits_memory`
- `publish_result_to_user`

---

## § 三类 Profile

| 类型 | 典型用途 | 默认特征 |
|------|---------|---------|
| Main | 与用户交互 | 可流式、可持久化 Session、通常允许更丰富工具 |
| Sub | 被主 Agent 同步委托 | 默认非流式、结果返回父 Agent、Ephemeral Session |
| Background | 长耗时后台任务 | `InvocationMode=detached`、异步完成、完成后独立通知 |

---

## § Profile 的使用方式

AgentLoop 每次收到消息时：

1. 通过 AgentResolver 或 mode 选出一个 `AgentProfile`
2. 读取该 Profile 的策略
3. 结合 Session、Context、Request Override 构建 `AgentRunSpec`
4. 交给 AgentRunner 执行

Profile 自己不执行任何逻辑。

---

## § 配置优先级

统一建议：

```text
App Defaults
< Provider Defaults
< Model Defaults
< AgentProfile Defaults
< Session Overrides
< Request Overrides
```

但只能覆盖软字段：

- `temperature`
- `max_tokens`
- `streaming`
- `reasoning_effort`

以下硬边界不可放宽：

- `allowed_tools`
- `provider_allowlist`
- `workspace_roots`
- `permission_profile`
- `max_iterations` 上限
- `cost_budget` 上限

---

## § 与相关模块的关系

| 模块 | AgentProfile 提供什么 | 不提供什么 |
|------|---------------------|-----------|
| AgentLoop | 构建一次 run 所需的默认策略 | Session、持久化、并发控制 |
| AgentRunner | 已解析后的执行参数来源 | 执行状态 |
| ProviderRegistry | 模型偏好与能力要求 | Provider Adapter 实例 |
| ToolExecutor | 工具白名单与超时策略 | 工具实现 |

InteractionPolicy 特别说明：

- AgentLoop 在 `resolve` 阶段确定当前 mode（用户指定或默认）
- 在 `build_context` 阶段将 mode 的 `system_prompt_addon` 注入上下文
- ToolExecutor 在 `filter_by_mode` 阶段根据 `allowed_tools` 过滤

---

## § 设计决策与权衡

| 决策问题 | 选择 | 放弃的替代方案 | 理由 |
|---------|------|--------------|------|
| Agent 是否持有 Provider？ | 否 | Agent 直接持有 `Arc<dyn Provider>` | Provider 是 adapter；Profile 只应描述模型偏好，不应绑定运行时实例 |
| Agent 是否持有 Session？ | 否 | Agent 内嵌消息历史与 turn | Session 与 Agent 生命周期不同，绑定后会污染定义层 |
| Main/Sub/Background 是否三套结构体？ | 否，共用 `AgentProfile` | 三个完全独立类型 | 差异主要是策略和 invocation mode，不值得复制整套结构 |
