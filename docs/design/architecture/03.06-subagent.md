# MindClaw 技术架构设计 — SubAgent

> 完整架构文档索引见 [README.md](./README.md) | Agent 核心见 [03-agent-loop.md](./03-agent-loop.md)

## SubAgent — 多 Agent 编排

所有 SubAgent **统一用 Markdown 文档定义**（YAML frontmatter + system prompt body），不设 Code SubAgent。内置 agent 通过 `include_str!` 编译期嵌入 `.md` 文件，与用户自定义 agent 共享同一解析和执行路径。

执行模式分两种：**Background**（后台）和 **Parallel**（并行）。

```
SubAgent (全部 Markdown 定义)
├── Background（后台）
│   ├── knowledge-distill.md    从对话中提炼知识笔记
│   ├── session-summarize.md    会话摘要生成
│   └── daily-summary.md        当日回顾
│
└── Parallel（并行）
    ├── explore.md              只读探索（Haiku 模型）
    ├── plan.md                 规划（继承主模型）
    └── research-assistant.md   用户自定义
```

### Agent Markdown 文件格式

```markdown
---
name: explore
description: "Read-only codebase exploration with focused search"
model: claude-3-5-haiku
mode: parallel
capabilities:
  allowed_tools: [filesystem]
  fs_scope: read_only [vault/]
  network: deny
  max_tool_calls: 8
  timeout_ms: 30000
---

# Explore Agent

You are a focused read-only exploration agent...
```

- **Markdown body** = system prompt
- **frontmatter** = 元数据 + capabilities + 默认执行模式
- `model` 可选，缺省则继承主代理模型
- `mode` 可选，缺省为 `parallel`

### 发现路径

| 优先级 | 来源    | 路径                        | 说明                                   |
| ------ | ------- | --------------------------- | -------------------------------------- |
| 1      | Builtin | `src-tauri/agents/*.md`     | 编译期嵌入（`include_str!`），开箱即用 |
| 2      | Project | `<project>/.agents/agents/` | 项目级自定义（需信任确认）             |
| 3      | User    | `~/.agents/agents/`         | 用户级共享                             |

冲突处理：Project > User > Builtin（同名高优先级覆盖，记录 warning）。

Project-level agents 首次扫描时需用户确认信任，防止恶意 prompt 注入。

### 执行机制

SubAgent 复用 Agent/AgentLoop 分离架构：

**共享 Agent（大脑）**：SubAgent 可共享主 Agent 的 `Arc<Agent>` 实例（同一 Provider、同一 ToolRegistry、同一 Observer），零额外开销。

**受限 Agent（独立大脑）**：当 SubAgent 需要不同的工具集或模型时，通过 AgentBuilder 构建独立的 Agent：

```rust
// 共享场景：SubAgent 与主 Agent 共用大脑
let sub_loop = SubAgentLoop::new(main_agent.clone(), sub_bus, sub_session_mgr);

// 受限场景：SubAgent 使用受限工具集
let sub_agent = AgentBuilder::new(config)
    .add_tool(filesystem_tool)      // 仅文件系统
    .build().await?;
let sub_loop = SubAgentLoop::new(sub_agent, sub_bus, sub_session_mgr);
```

每个 SubAgent 执行时创建 **mini AgentLoop**：

1. Markdown body 作为 system prompt
2. `SubAgentContext.parameters` 作为 user message
3. 使用 frontmatter 中指定的 model 和 capabilities
4. 运行受限工具循环（仅 `allowed_tools`，受 `max_tool_calls` / `timeout_ms` 约束）
5. 收集最终 assistant 文本作为 `SubAgentResult.output`

### 执行模式

#### Background（后台）

- Run 完成后由 `dispatch_background()` 入队
- **持久化任务队列**：写入 durable store，进程重启后可恢复
- 失败按指数退避重试，达上限转 `Failed`
- 支持 `dedupe_key` 去重
- 结果通过 Artifact 落盘或记录日志，不返回给主代理

#### Parallel（并行）

- Run 内由 LLM 通过 Agent-as-Tool 调用，或由 AgentLoop 显式启动
- 与其他工具调用共享 Semaphore 并发配额
- 结果返回给主代理，参与下一轮 LLM 推理
- 受 `timeout_ms` 约束，超时返回错误结果

### 核心类型

```rust
// src-tauri/src/agent/sub_agent.rs

/// SubAgent 执行模式
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SubAgentMode {
    Background,
    Parallel,
}

/// 从 Markdown frontmatter 解析的 SubAgent 定义
pub struct SubAgentDef {
    pub name: String,
    pub description: String,
    pub system_prompt: String,          // Markdown body
    pub mode: SubAgentMode,
    pub model: Option<String>,
    pub capabilities: CapabilityProfile,
    pub source_path: Option<PathBuf>,   // None = builtin
}

/// SubAgent 运行时上下文（执行时传入）
pub struct SubAgentContext {
    pub task_id: String,
    pub session_id: String,
    pub parameters: serde_json::Value,
    pub cancel: CancellationToken,
}

/// SubAgent 执行结果
pub struct SubAgentResult {
    pub success: bool,
    pub output: serde_json::Value,
    pub logs: Vec<String>,
}
```

### SubAgentRegistry

```rust
pub struct SubAgentRegistry {
    agents: HashMap<String, Arc<SubAgentDef>>,
    job_store: Arc<dyn SubAgentJobStore>,
    background_queue: Arc<dyn SubAgentQueue>,
}

impl SubAgentRegistry {
    /// 从文件系统发现并注册（builtin + project + user）
    pub async fn discover(&mut self, project_root: &Path) -> Result<(), AppError>;

    /// 后台派发（durable enqueue）
    pub async fn dispatch_background(&self, name: &str, ctx: SubAgentContext) -> Result<JobId, AppError>;

    /// 并行执行（等待结果返回）
    pub async fn dispatch_parallel(&self, name: &str, ctx: SubAgentContext) -> Result<SubAgentResult, AppError>;

    /// 列出所有可用 SubAgent（供 operations list 返回）
    pub fn list(&self) -> Vec<SubAgentInfo>;
}
```

dispatch 前校验：

- `capabilities.allowed_tools` 满足最小权限
- `fs_scope` / `network` 与当前 channel 和运行模式兼容
- `timeout_ms` / `max_tokens` / `max_tool_calls` 在系统上限内

### Agent-as-Tool：SubAgentTool

将 SubAgent 包装为 Tool，供 LLM 在 Parallel 模式下调用：

```rust
pub struct SubAgentTool {
    agent_name: String,
    registry: Arc<SubAgentRegistry>,
}

#[async_trait]
impl Tool for SubAgentTool {
    fn name(&self) -> &str;  // 命名空间：agent__{name}
    async fn execute(&self, input: ToolInput) -> AppResult<ToolOutput>;
}
```

Parallel SubAgent 默认**可发现**（非常驻），通过 `operations list` 在 `agents` 命名空间暴露。

### 与 Skill 的关系

| 维度     | Skill                              | SubAgent                                  |
| -------- | ---------------------------------- | ----------------------------------------- |
| 定位     | Prompt-side guidance（上下文注入） | Independent execution（独立执行）         |
| 上下文   | 注入主会话 system prompt           | 独立 system prompt + 工具循环             |
| 输出     | 影响主代理行为                     | 产出独立结果（返回主代理或落盘）          |
| 文件格式 | `SKILL.md`（三级披露）             | `AGENT.md`（frontmatter + system prompt） |
| 发现路径 | `.agents/skills/`                  | `.agents/agents/`                         |

Skill 的 `context: fork` 是语法糖：Skill content 作为 system prompt → 创建临时 SubAgent → `dispatch_parallel` → 返回结果给主会话。

### CapabilityProfile（安全边界）

```rust
pub struct CapabilityProfile {
    pub allowed_tools: Vec<String>,
    pub fs_scope: FsScope,
    pub network: NetworkPolicy,
    pub approval_policy: ApprovalPolicy,
    pub max_tokens: Option<u32>,
    pub max_tool_calls: u32,
    pub timeout_ms: u64,
    pub max_retries: u8,
}
```

Markdown SubAgent 的 capabilities 从 frontmatter 解析；Code SubAgent 在注册时声明。
