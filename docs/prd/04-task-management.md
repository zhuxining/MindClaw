> **Status**: `draft`

# 任务管理

→ 架构关联：[08-desktop-frontend.md](../design/arch/08-desktop-frontend.md)  
→ 父文档：[00-overview.md](00-overview.md)

## 背景与目标

**背景**：任务是 MindClaw 中的一等公民——有独立数据结构（状态、截止日、优先级），而非日记中的普通文字行。任务的真相源是 `{vault}/tasks/*.md`，SQLite 为查询索引。用户通过右侧 Tasks 面板随时感知任务状态，通过 Chat 对话创建任务，或在专属任务视图中管理。

**目标**：定义任务的创建、查看、状态流转、在工作区中的展示位置和行为。

## 功能描述

### US-01 查看任务列表

作为用户，我希望在右侧 Tasks 面板看到当前相关任务，以便随时了解今日和进行中的任务。

**验收标准**：

Given 用户处于工作区任意状态  
When 右侧 Tasks 面板可见  
Then Tasks 面板显示以下分组（按优先级显示）：

  1. 今日到期（`due_date` 为今日的待办和进行中任务）
  2. 进行中（`status` 为 `in_progress` 且不属于今日到期）
  3. 待办（`status` 为 `todo` 且无截止日或截止日为未来）

- [ ] 每条任务显示：复选框、标题、优先级指示色（高=红/中=橙/低=灰）
- [ ] 分组标题显示该分组的任务数量
- [ ] 已完成任务（`status` = `done`）不在 Tasks 面板中显示
- [ ] Tasks 面板内容支持垂直滚动

**优先级**：P0

---

### US-02 更新任务状态

作为用户，我希望在 Tasks 面板中直接勾选任务为完成，以便快速标记执行结果。

**验收标准**：

Given 用户看到 Tasks 面板中某个待办任务  
When 用户点击该任务的复选框  
Then 该任务状态从 `todo` 或 `in_progress` 变更为 `done`，该任务从 Tasks 面板中消失

Given 任务状态变更成功  
When 变更写入文件和索引  
Then 对应的 Markdown 文件 Frontmatter 中 `status` 字段同步更新

- [ ] 状态变更在 300ms 内完成（包括文件写入）
- [ ] 状态变更失败时（如文件写入错误），复选框回滚到变更前状态，并显示错误提示

**优先级**：P0

---

### US-03 创建新任务

作为用户，我希望从 Tasks 面板创建新任务，以便快速记录待执行事项。

**验收标准**：

Given 用户在 Tasks 面板中点击"+"按钮  
When 弹出创建任务对话框  
Then 对话框包含：标题输入框（必填）、优先级选择（高/中/低，默认中）、截止日期选择（可选）

Given 用户填写任务标题并确认  
When 点击"创建"按钮  
Then 新任务创建成功，在 `{vault}/tasks/` 下生成对应 Markdown 文件，Tasks 面板实时更新显示新任务

Given 用户点击"创建"但标题为空  
When 尝试提交  
Then 标题输入框显示红色边框和"请输入任务标题"提示，不关闭对话框

- [ ] 创建对话框打开时标题输入框自动获得焦点
- [ ] Enter 键触发创建（等同于点击"创建"按钮）
- [ ] Escape 键取消创建并关闭对话框

**优先级**：P0

---

### US-04 查看任务详情

作为用户，我希望点击任务后查看完整的任务信息，以便了解任务的详细内容和备注。

**验收标准**：

Given 用户在 Tasks 面板中点击某任务标题  
When 点击操作触发  
Then 右侧 Tasks 面板展开该任务的详情视图，显示：标题、状态、优先级、截止日期、正文内容（Markdown 渲染）

- [ ] 任务详情视图在 Tasks 面板内展开（不打开新窗口，不占用中央区域）
- [ ] 详情视图包含"返回列表"按钮，点击后回到任务列表
- [ ] 任务正文（Markdown 文件 body 部分）以只读 Markdown 视图渲染

**优先级**：P1

---

### US-05 通过对话创建任务

作为用户，我希望在 Chat 中说"帮我记个任务"，Agent 自动创建任务并显示在 Tasks 面板中，以便不打断对话流程就完成任务创建。

**验收标准**：

Given 用户在 Chat 中发送包含任务创建意图的消息（如"帮我记个任务：明天和产品开会"）  
When Agent 识别意图并执行创建任务工具  
Then Tasks 面板实时更新，新任务出现在列表中；Agent 在 Chat 中回复确认消息，包含任务标题和状态

- [ ] 任务通过 Chat 创建后，与用户手动创建的任务具有相同的数据结构
- [ ] Agent 创建的任务自动归入"待办"分组（`status` = `todo`）

**优先级**：P1

## 范围界定

**In Scope**：

- 右侧 Tasks 面板（分组显示：今日/进行中/待办）
- 任务状态切换（勾选为完成）
- 快速创建任务（对话框，含标题/优先级/截止日）
- 任务详情查看（面板内展开）
- 通过 Chat 对话创建任务

**Out of Scope**：

- 任务编辑（修改标题、描述）：Phase 2 功能，需要完整编辑表单；Phase 1 创建后不支持修改
- 任务删除/取消：Phase 2，需要状态转换为 `cancelled`，以及对应的 UI 确认流程
- 子任务：任务嵌套是独立的数据模型变更，排除
- 任务标签过滤：Phase 2 功能，排除
- 任务与日历同步：不接入系统日历，排除；提醒由 Agent 驱动而非日历

## 非功能需求

- 任务列表加载时间 ≤ 500ms（从 SQLite 索引查询）
- 任务状态更新（复选框点击到 UI 响应）≤ 100ms（乐观更新）
- 任务文件写入失败时必须回滚 UI 状态，不出现数据不一致
